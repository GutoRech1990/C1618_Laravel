#Démarrage du projet
Pour démarrer le projet, exécutez la commande suivante dans le terminal :
php artisan serve

#TailwindCSS
Le tailwindCSS a eté instalé via CDN, alors il n'est pas necessarie de l'initializar via terminal

#Remplissage de la base de données
Une fois que les factories ont eté dejá creé dans la dossier database/factories, 
pour populer la base de donnés il faut seulement le command suivant dans le terminal:
php artisan db:seed